rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}

buildscript {
    repositories {  // ✅ Add this missing block
        google()
        mavenCentral()
        gradlePluginPortal()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.3.0'  // Update to latest stable version
        classpath 'com.google.gms:google-services:4.3.15' // Ensure this is inside buildscript
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
